---
tools: [queryDB]
input:
  schema:
    query: string
    schema: string
output:
  schema:
    data: string
---
Using provided schema and tool `executeQuery` output all required information to answer user's query. It will be provided to another agent, so make all field names clear and descriptive. You can append any related text info to the result as well
query: {query}
schema: {schema}
