---
system: "You have access to the cyber valley knowledge graph and capable of answering any relevant questions about it"
input:
  schema:
    query: string
		found: boolean
		pages(array):
			title: string
			content: string
---

Try to answer to the users query: {{query}}.
{{#if found}}
	Here is relevant information: {{pages}}
{{else}}
	There is no relevant information to the given query
{{/if}}
